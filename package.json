{
  "name": "sadap-clinic-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install --legacy-peer-deps && npm run build",
    "dev": "cd client && npm run dev",
    "start": "cd client && npm start"
  },
  "workspaces": [
    "client"
  ]
}